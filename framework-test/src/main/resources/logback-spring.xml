<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!-- ===============================
         通过 Spring 属性加载应用名，确保 ${spring.application.name} 生效
         =============================== -->
    <springProperty scope="context" name="appName" source="spring.application.name"/>

    <!-- ===============================
         控制台日志
         =============================== -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ===============================
         文件日志（按天滚动）
         =============================== -->
    <appender name="FileInfo" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 使用 springProperty 的 appName -->
        <file>logs/${appName:-application}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 按天滚动 + 每天最大文件大小 50MB -->
            <fileNamePattern>logs/${appName:-application}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>20GB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="FileError" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${appName:-application}-error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/${appName:-application}-error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ===============================
         Root 日志
         =============================== -->
    <root level="INFO">
        <appender-ref ref="Console"/>
        <appender-ref ref="FileInfo"/>
        <appender-ref ref="FileError"/>
    </root>

    <!-- ===============================
         自定义包日志级别
         =============================== -->
    <logger name="com.example.demo" level="DEBUG" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="FileInfo"/>
        <appender-ref ref="FileError"/>
    </logger>

</configuration>
