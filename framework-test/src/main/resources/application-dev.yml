spring:
  datasource:
    # PostgreSQL 数据源配置
    url: jdbc:postgresql://117.72.94.34:5432/cc  # 数据库连接地址
    driver-class-name: org.postgresql.Driver      # JDBC 驱动
    username: root                                # 数据库用户名
    password: 4396                                # 数据库密码
  data:
    redis:
      # Redis 基本连接配置
      host: 117.72.94.34          # Redis 服务 IP
      port: 6379                  # Redis 端口
      password: 4396              # Redis 密码
      database: 0                 # 使用的 Redis 数据库索引
      timeout: 10000ms             # 连接超时时间（毫秒）
  ai:
    openai:
      api-key: 1234567  # 从环境变量读取
      chat:
        options:
          model: gpt-4o            # 模型名称
          temperature: 0.8         # 随机程度，可根据业务调整
          maxTokens: 1000          # 可选，限制输出长度


logging:
  level:
    com.bill.test: DEBUG